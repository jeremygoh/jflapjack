<% @note.errors.full_messages.each do |message| %>
    <p><%= message %></p>
<% end %>

<h2>Create New Note</h2>
<%= form_for @note do |f| %>

<%= f.label :caption %>
<%= f.text_field :caption %>

<%= f.label :body %>
<%= f.text_area :body %>

<%= f.submit %>

<% end %>